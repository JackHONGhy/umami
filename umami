#!/usr/bin/env bash
# Umami Docker 一键部署脚本（wget 可直接执行）
# 系统需已有 docker & docker compose

set -euo pipefail

UMAMI_DIR="/opt/umami-docker"
COMPOSE_FILE="${UMAMI_DIR}/docker-compose.yml"
CONFIG_FILE="/etc/umami-docker.conf"
UMAMI_PORT=3000

###############################################
# 工具函数
###############################################
require_root() {
  if [[ "$EUID" -ne 0 ]]; then
    echo "❌ 请使用 root 或 sudo 运行脚本"
    exit 1
  fi
}

pause() {
  echo
  read -rp "👉 按回车继续..." _
}

print_header() {
  clear
  echo "===================================================="
  echo "           Umami Docker 一键管理脚本"
  echo "===================================================="
  echo
}

get_ip() {
  ip=$(hostname -I | awk '{print $1}')
  [[ -z "$ip" ]] && ip="localhost"
  echo "$ip"
}

###############################################
# 配置加载 / 创建
###############################################
load_or_create_config() {
  if [[ -f "$CONFIG_FILE" ]]; then
    . "$CONFIG_FILE"
  else
    DB_PASSWORD=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 20)
    cat > "$CONFIG_FILE" <<EOF
DB_PASSWORD="$DB_PASSWORD"
UMAMI_PORT="$UMAMI_PORT"
EOF
    chmod 600 "$CONFIG_FILE"
  fi
}

###############################################
# 生成 docker-compose.yml
###############################################
generate_compose_file() {
  mkdir -p "$UMAMI_DIR"

cat > "$COMPOSE_FILE" <<EOF
version: '3.9'

services:
  umami-db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - umami-db-data:/var/lib/postgresql/data

  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    restart: always
    ports:
      - "${UMAMI_PORT}:3000"
    depends_on:
      - umami-db
    environment:
      DATABASE_URL: postgresql://umami:${DB_PASSWORD}@umami-db:5432/umami

volumes:
  umami-db-data:
EOF
}

###############################################
# 安装 Umami
###############################################
install_umami() {
  print_header
  echo "📦 正在部署 Umami（Docker 模式）..."

  generate_compose_file

  cd "$UMAMI_DIR"
  docker compose pull
  docker compose up -d

  ip=$(get_ip)

  echo
  echo "🎉 安装完成！"
  echo "📍 访问地址： http://${ip}:${UMAMI_PORT}"
  echo "🔐 默认账号：admin"
  echo "🔐 默认密码：umami"
  echo

  pause
}

###############################################
# 更新 Umami
###############################################
update_umami() {
  print_header

  if [[ ! -f "$COMPOSE_FILE" ]]; then
    echo "❌ 未检测到安装目录：$UMAMI_DIR"
    pause
    return
  fi

  echo "⬆️ 正在更新 Umami 镜像..."
  cd "$UMAMI_DIR"
  docker compose pull
  docker compose up -d --force-recreate

  echo
  echo "✅ 更新完成！"
  pause
}

###############################################
# 卸载 Umami
###############################################
uninstall_umami() {
  print_header
  echo "⚠️ 卸载会删除所有数据！"
  read -rp "输入 YES 继续卸载：" confirm

  [[ "$confirm" != "YES" ]] && echo "取消卸载。" && return

  cd "$UMAMI_DIR" 2>/dev/null || true

  echo "🛑 停止容器..."
  docker compose down -v || true

  echo "🗑 删除目录..."
  rm -rf "$UMAMI_DIR"

  echo "🧾 删除配置..."
  rm -f "$CONFIG_FILE"

  echo
  echo "✅ 卸载完成！"
  pause
}

###############################################
# 清理缓存
###############################################
clean_cache() {
  print_header
  echo "🧹 正在清理 Docker 缓存..."
  docker system prune -f || true
  echo "✅ 清理完成"
  pause
}

###############################################
# 主菜单
###############################################
main_menu() {
  require_root
  load_or_create_config

  while true; do
    print_header
    echo "请选择操作："
    echo
    echo "  1) 安装 Umami"
    echo "  2) 更新 Umami"
    echo "  3) 卸载 Umami"
    echo "  4) 清理缓存"
    echo "  0) 退出"
    echo
    read -rp "请输入编号：" op

    case "$op" in
      1) install_umami ;;
      2) update_umami ;;
      3) uninstall_umami ;;
      4) clean_cache ;;
      0) exit 0 ;;
      *) echo "❌ 输入错误，请重试"; sleep 1 ;;
    esac
  done
}

main_menu
